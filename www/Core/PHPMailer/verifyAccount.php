<h1>Votre adresse email a bien été vérifié!</h1>

<?php

// $pdo = new \PDO("mysql:host=" . DBHOST . ";port=" . DBPORT . ";dbname=" . DBNAME, DBUSER, DBPWD);

// $iduser = $_GET['id'];
// $emailToken = $_GET['emailToken'];
// $recupUser = $pdo->prepare('SELECT * FROM zdek_user WHERE id = ? AND emailToken = ?');
// $recupUser->execute(array($iduser, $emailToken));

// if ($recupUser->rowCount() > 0) {
//     $userInfo = $recupUser->fetch();
//     if ($userInfo['status'] != 1) {
//         $updateStatus = $pdo->prepare('UPDATE zdek_user SET status = ? WHERE id = ?');
//         $updateStatus->execute(array(1, $iduser));
//     }
// }

// echo 'L\'id user est ' . $iduser . ' et le token de l\'email est : ' . $emailToken;
